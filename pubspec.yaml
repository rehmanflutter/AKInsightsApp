name: ak_insights_app
description: AK Insights - Instagram Analytics Style Web App (Demo UI)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  url_launcher: ^6.2.1
  flutter_secure_storage: ^9.0.0
  webview_flutter: ^4.4.2
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
